
<body>
    <section class="chat-container">
        <div class="card">
            <div class="card-body">
                <div class="mb-md-5 mt-md-4 pb-1">
                    <h2 class="card-title">Perfil</h2>
                    <div class="card-text">
                        {{#if user.isGithub}}
                        <p>Nombre/Usuario: {{{user.first_name}}}</p>
                        <p>Email: {{{user.email}}}</p>
                        <p>Ingreso con: Github</p>
                        {{else}}
                        <p>Nombre: {{{user.first_name}}}</p>
                        <p>Apellido: {{{user.last_name}}}</p>
                        <p>Email: {{{user.email}}}</p>
                        <p>Edad: {{{user.age}}}</p>
                        <p>Ingreso con: LocalPassport</p>
                        {{/if}}
                    </div>
                    <button onclick="location.href='/products'">Ir a: Lista de
                        Productos</button>
                    <p class="mt-3">Serás redirigido a la página de productos en 10 segundos.</p>
                </div>
            </div>
        </div>
    </section>
</body>

<script>
    setTimeout(function () {
        window.location.href = '/products';
    }, 10000);
</script>